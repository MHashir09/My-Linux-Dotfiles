return {
  'vyfor/cord.nvim',
  opts = {
    display = {
      theme = 'minecraft',
      -- Optional: pick a flavor if you want (most themes have dark/light/accent variants)
      -- flavor = 'dark',   -- defaults to whatever the theme's default is (often 'dark')
    },
  },
}
